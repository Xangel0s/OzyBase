# OzyBase Environment Configuration

# PostgreSQL connection string
DATABASE_URL=postgres://ozyuser:ozypassword123@localhost:5432/Ozydb?sslmode=disable

# Enterprise Variables (Alternative setup)
DB_HOST=localhost
DB_PORT=5432
DB_USER=ozyuser
DB_PASSWORD=ozypassword123
DB_NAME=Ozydb
DB_SSLMODE=disable

# Server port
PORT=8090

# JWT Secret Key
JWT_SECRET=super-secret-key-change-it
